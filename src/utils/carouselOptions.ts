import { EmblaOptionsType } from 'embla-carousel'

export const CAROUSEL_OPTIONS: EmblaOptionsType = {
  align: 'start',
  skipSnaps: true,
  slidesToScroll: 1,
  loop: true,
}

export const CAROUSEL_OPTIONS_BLOG: EmblaOptionsType = {
  align: 'start',
  skipSnaps: true,
  slidesToScroll: 1,
  loop: true,
}
